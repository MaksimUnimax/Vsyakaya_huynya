name: Ludka Vela Fill Audit

on:
  push:
    branches:
      - 'лудка'
    paths:
      - 'repo-radar/ludka/audits/vela_fill_audit.py'
      - 'repo-radar/ludka/audits/vela_outcome_audit.py'
      - 'repo-radar/ludka/audits/vela_series_fee_audit.py'
      - '.github/workflows/ludka-vela-fill-audit.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install audit dependencies
        run: python -m pip install --disable-pip-version-check pandas pyarrow numpy requests
      - name: Run committed-parquet audit
        env:
          AUDIT_OUT: audit-out
        run: python repo-radar/ludka/audits/vela_fill_audit.py | tee audit-console.txt
      - name: Run public-settlement outcome audit
        env:
          AUDIT_OUT: audit-out
        run: python repo-radar/ludka/audits/vela_outcome_audit.py | tee outcome-console.txt
      - name: Run series and fee arithmetic audit
        env:
          AUDIT_OUT: audit-out
        run: python repo-radar/ludka/audits/vela_series_fee_audit.py | tee series-fee-console.txt
      - name: Show machine reports
        if: always()
        run: |
          test -f audit-out/audit_report.md && cat audit-out/audit_report.md || true
          test -f audit-out/outcome_audit.md && cat audit-out/outcome_audit.md || true
          test -f audit-out/series_fee_audit.md && cat audit-out/series_fee_audit.md || true
          test -f audit-out/probe.json && cat audit-out/probe.json || true
      - name: Upload audit evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: vela-fill-audit
          if-no-files-found: warn
          path: |
            audit-out/audit_report.md
            audit-out/audit_report.json
            audit-out/kalshi_outcomes.json
            audit-out/outcome_audit.md
            audit-out/outcome_audit.json
            audit-out/outcomes.json
            audit-out/series_fee_audit.md
            audit-out/series_fee_audit.json
            audit-out/probe.json
            audit-out/fill_calibration_btc.parquet
            audit-console.txt
            outcome-console.txt
            series-fee-console.txt
